{
  "name": "hackitfintech",
  "version": "1.0.0",
  "description": "FinTech Application with React Frontend and FastAPI Backend",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npx vite",
    "backend": "cd asset-backend && python -m uvicorn app:app --reload --port 8000",
    "install-all": "npm install && cd frontend && npm install && cd ../asset-backend && pip install fastapi uvicorn pandas yfinance joblib python-multipart scikit-learn"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
